# 更新日志

本文件记录项目的重要变更。

## [待发布]

### 性能
- **首页打开提速**：38MB 全量字库不再随应用打包/启动加载。拆分为 435KB 元数据索引（`chars_index.json`，首页/搜索/筛选所需）+ 38 个笔画分片（详情页按需加载并缓存），安装包体积同步减少约 35MB。

### 新增
- 笔画名称权威数据：接入 cnchar-order 字典（MIT）生成 `stroke_names.json`，6807 字直接使用官方笔顺名称；歧义对（横撇/横钩、斜钩/卧钩等）由几何消解；其余字回退到重构后的 `StrokeClassifier` 几何分类器。
- 组词库扩充：基于 CC-CEDICT（CC BY-SA 4.0）生成 `words.json`，6122 字提供带调拼音组词（儿童内容过滤 + 目标字打头优先排序）。
- 中文释义：接入新华字典数据（pwxcoo/chinese-xinhua, MIT）生成 `definitions_zh.json`，7451 字显示中文解释，英文释义降为补充。
- 笔顺动画时长归一化：按中轴长度加权（最长笔=1.0、下限 0.3），"点"等短笔明显更快。
- 浏览器返回键与 Flutter 路由联动：URL 改用 pushState 并监听 popstate，前进/后退可在站内导航。
- 新增回归基线测试：32 字官方笔顺金样集、揭示笔刷边界覆盖率（16 字全边界点零缺口）、数据资产契约校验。

### 变更
- 详情页 URL 同步收敛至 `NavigatorObserver` 统一处理；移除各页面手动同步与"母"字硬编码笔顺。

### 修复
- 修复播放动画红色揭示层无法完整覆盖灰色描边的问题（笔刷加宽 + 中轴均匀重采样 + 末端延伸）。

### 新增
- 新增权威离线汉字笔画数据集：`/Users/jyy/Documents/bihua/assets/data/chars_3500.json`（9565 个 CJK 条目，含笔画轮廓与中线数据）。
- 新增数据来源与许可说明：`/Users/jyy/Documents/bihua/assets/data/LICENSE.md`。
- 新增 Web 与 macOS 运行目标，支持本地快速调试：`flutter run -d chrome` / `flutter run -d macos`。

### 变更
- 调整笔顺渲染流程：
  - 底图使用真实笔画轮廓渲染；
  - 当前笔画使用红色高亮；
  - 支持坐标系 Y 轴翻转（适配不同数据源坐标方向）。
- 调整播放行为：
  - 进入详情页自动播放；
  - 默认播放速度调慢（`speed = 0.6`）；
  - 每次详情页会话隔离，避免复用旧播放状态。
- 调整字库加载策略：默认不再自动合成占位字（`minDictionarySize = 0`）。
- 更新 README，补充真实运行行为、数据来源与缓存排查说明。
- 文档统一中文表述，并补充 Android 安装运行前置步骤。

### 修复
- 修复详情页字形与笔顺显示不一致问题。
- 修复详情页初始状态异常（整字全黑/播放状态残留）问题。
- 修复渲染与播放改动后的多处测试不一致问题。

## [0.1.0] - 2026-02-19

### 新增
- 初始化 Flutter 应用结构（首页 + 详情页）。
- 支持 1~20 汉字查询、拼音/笔画/部首筛选入口、笔顺播放控制。
- 新增输入清洗、字库查询、首页与详情页交互等测试用例。
